## Start dev mode

```bash
yarn start
```

## Build local app bundle

```bash
yarn build
```

## Build docker container

```bash
yarn docker:build
```

This command create an container with tag `visit-kchr-frontend`


## Run docker container

```bash
yarn docker:run
```

Runs the created container with `visit-kchr-frontend` tag

